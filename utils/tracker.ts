interface capture {
    id:string;
    status:number
    date:Date
}

interface Tracker {
    url: string,
    interval: number,
    user_id: string,
    id: string
    active: boolean,
    name: string,
}

